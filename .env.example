# Database Configuration
POSTGRES_USER=bot
POSTGRES_PASSWORD=botpw
POSTGRES_DB=trading
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Interactive Brokers TWS Configuration
TWS_HOST=172.17.0.1
TWS_PORT=7497                # 7497 for paper trading, 7496 for live
USE_PAPER=1                  # 1 for paper trading, 0 for live
ENABLE_LIVE=0                # 1 to enable live trading (requires additional safety checks)
DRY_RUN=1                    # 1 for simulation mode, 0 for actual execution

# Market Data Configuration
DEFAULT_SYMBOLS=AAPL,MSFT,SPY
MAX_SUBSCRIPTIONS=50         # Hard cap for live market data subscriptions
BAR_SIZE=1 min
WHAT_TO_SHOW=TRADES
RTH=1                        # 1 for regular trading hours only, 0 for extended hours
LOOKBACK=30 D

# Historical Data Configuration
MAX_HIST_REQUESTS_PER_MIN=30
HIST_BAR_SIZES=1 min,5 mins,1 day

# Client ID Management
TWS_CLIENT_ID_BASE=10
RECONNECT_BACKOFF_MIN=1
RECONNECT_BACKOFF_MAX=30

# Backtest Configuration
BT_COMM_PER_SHARE=0.005
BT_MIN_COMM_PER_ORDER=1.0
BT_DEFAULT_SLIPPAGE_TICKS=1
BT_TICK_SIZE_US_EQUITY=0.01

# Logging Configuration
LOG_LEVEL=INFO
LOG_FORMAT=json

# Development Settings
DEBUG=0
DEVELOPMENT_MODE=0
